# Compatibility install entry point.
#
# Dependency versions are declared only in pyproject.toml. Keeping a second
# hand-maintained list here caused version drift, so `pip install -r
# requirements.txt` now delegates to the canonical editable install.
-e .
