# A股多标的垂直分时对比Web应用 完整项目规划（Plan）

## 一、项目定位与核心目标
### 1. 核心价值
解决传统行情软件单屏难以同时对比多个标的分时走势的痛点，通过**垂直并列+左右分屏双布局**+**涨跌幅归一化对比**实现板块与个股的高效强弱对照，配合后台自动行情刷新与亮暗双主题，打造轻量、高效、贴合职业看盘习惯的本地分时对比工具。

### 2. 核心能力
基于通达信公开行情协议，支持A股全类型板块、沪深个股的分时数据查询与可视化对比，主打**轻量本地部署、后台静默刷新、板块-个股分屏对标、亮暗双主题适配**，支持混合排列与分屏对照两种查看模式。

### 3. 适用场景
- 日内交易盯盘：同时监控多个关注板块、核心个股的实时走势
- 板块-个股联动对比：左右分屏对照板块与个股走势，验证个股强弱与板块相关性
- 板块强弱排序：直观对比日内涨幅、拉升力度、抗跌性
- 热点持续性观察：跟踪概念板块及龙头个股的日内分时量价变化
- 跨板块联动分析：观察上下游、关联板块的走势传导关系

### 4. 非目标边界
- 不提供实盘交易功能
- 不提供Level-2逐笔、十档盘口等付费行情
- 不做深度基本面、财务数据分析
- 初期不支持多日连续K线，专注日内分时维度

---

## 二、整体技术架构设计
### 1. 技术栈选型
| 层级 | 选型 | 选型理由 |
|---|---|---|
| 数据源 | easy-tdx | 原生通达信协议，免费、板块+个股数据覆盖全、接入成本低 |
| 后端框架 | FastAPI | 轻量异步、性能优异、自带接口文档，支持后台定时任务 |
| 缓存层 | 内存字典 + 本地文件缓存 | 内存级TTL缓存承载实时数据，文件级缓存实现当日数据持久化 |
| 前端框架 | 原生HTML/CSS + 轻量JS | 无需构建工具，开箱即用，降低部署复杂度 |
| 图表库 | Lightweight Charts | 原生支持亮暗主题，渲染性能强，完美适配多图并列场景 |
| 部署方式 | 本地单进程启动 | 浏览器访问，无需公网服务器，配套Windows静默一键启动脚本 |

### 2. 分层架构
1.  **数据接入层**：封装 easy-tdx 连接管理、节点优选、故障重试，统一适配板块与个股接口
2.  **后台刷新层**：独立后台定时线程，按配置周期自动拉取行情并更新缓存，与前端请求解耦
3.  **缓存层**：内存缓存存储最新分时数据，本地文件缓存存储当日全量数据，作为前端数据唯一来源
4.  **API服务层**：提供标准化REST接口，向前端屏蔽底层刷新逻辑与标的类型差异
5.  **前端展示层**：负责双布局渲染、图表交互、主题切换、联动逻辑、用户配置管理

### 3. 数据流向
通达信行情服务器 → 后台定时线程拉取数据 → 更新内存缓存与本地文件缓存 → 前端定时拉取缓存数据 → 分时图渲染与状态更新

---

## 三、核心功能模块详细设计
### 模块1：标的管理与选择
- **双标的池设计**：分为板块池与个股池，支持一键切换浏览
  - 板块池：一级行业、二级行业、概念板块、风格板块、地区板块五大分类
  - 个股池：沪深A股全量标的，支持按市场（沪市/深市/创业板/科创板）筛选
- **统一搜索能力**：支持按名称、代码模糊搜索，自动匹配板块与个股结果
- **混合添加/移除**：板块与个股可同时勾选，一键加入对比列表，支持批量移除
- **自选标的组**：支持保存常用的板块+个股组合，下次打开一键加载
- **自由排序**：支持拖拽上下拖动，板块与个股可自由穿插调整垂直排列顺序
- **列表信息展示**：每条目显示标的类型标识、代码、当前涨幅、涨跌额等基础信息
- **分屏自动归类**：切换至分屏对比模式时，自动将板块归入左栏、个股归入右栏
- **个股关联板块快捷添加**：右键个股条目可查看其所属行业、核心概念板块，支持一键添加至对比列表

### 模块2：分时图双布局展示
#### 布局模式一：全标的垂直混排模式
- 所有选中标的（板块+个股）沿垂直方向并列排列，默认高度均分，支持单图高度拖拽调整
- 适合多标的批量横向对比、强弱排序场景

#### 布局模式二：板块-个股左右分屏对比模式
- 页面沿水平方向分为左右两栏，左栏固定展示板块分时，右栏固定展示个股分时
- 左右两栏内部均采用垂直并列排列，各自支持独立滚动、高度调节，分栏比例支持拖拽调整
- 专为「板块走势 vs 个股走势」对照场景设计，直观验证个股与板块的联动性、相对强弱
- 支持跨栏联动：鼠标悬停任意图表，左右两栏所有图表同步显示对应时间点的十字线与数据

#### 单图通用核心要素（板块/个股通用）
- 分时价格线（涨跌配色区分）
- 底部成交量柱状图
- 昨收基准虚线
- 图头信息：标的类型标识、名称、当前价、涨跌幅、涨跌额
- 时间轴、价格轴刻度
- **Y轴双模式切换**：支持绝对价格模式 / 涨跌幅归一化模式，后者统一以昨收为基准显示涨跌幅百分比，实现跨标的直观对比强弱
- 集合竞价数据展示：包含9:25开盘价数据，符合真实看盘时间线
- 午间休市标记：时间轴上用灰色区块标记11:30-13:00休市时段

#### 个股专属增强要素
- 分时均价线
- 成交量柱按涨跌着色
- 可选显示涨停/跌停价基准线

#### 通用交互能力
- 多图联动：鼠标悬停同步十字线，左侧面板对应标的同步高亮
- 缩放与平移：支持单图鼠标滚轮缩放、拖动平移，可开启全图时间轴同步缩放
- 涨跌配色：支持红涨绿跌/绿涨红跌两种配色方案切换，与全局主题适配

### 模块3：后台自动刷新与时效控制
- **后台静默更新机制**：
  - 后端启动独立后台线程，按配置的刷新间隔自动拉取所有已订阅标的的分时数据
  - 数据更新全程在后台完成，无需前端触发，不阻塞用户操作
  - 更新完成后自动覆盖内存缓存，保证缓存始终是最新数据
- **交易日历与分时段精细化控制**：
  - 内置A股交易日历，自动判断当日是否为交易日，非交易日完全休眠不发起请求
  - 开盘前（9:00-9:25）：仅维持连接预热，不启动高频刷新
  - 午间休市（11:30-13:00）：暂停刷新，保留上午完整数据
  - 收盘后（15:00后）：锁定当日完整数据，停止自动刷新
- **冷启动首屏加速**：启动后优先加载前8个标的的数据，保证首屏快速渲染，剩余标的后台异步串行加载
- **前端数据展示**：
  - 前端仅定时从后端缓存拉取数据，不直接触发通达信请求
  - 左侧面板状态区常驻显示「最后数据更新时间」，精确到秒
  - 数据刷新时状态区有轻微视觉提示，不干扰看盘
- **可配置刷新间隔**：默认30秒，支持15秒/30秒/60秒档位切换，不提供低于10秒选项以控风险
- **手动强制刷新**：顶部工具栏提供一键刷新按钮，触发后台立即执行一次全量数据更新
- **错峰刷新机制**：多标的数据串行请求，超过10个标的自动分批，避免同一瞬间集中发请求触发风控

### 模块4：辅助分析功能
- **混合涨幅排序**：对比列表支持按当前涨幅一键正/倒序排序，快速识别日内强弱
- **大盘基准叠加**：可选叠加上证指数/深证成指分时线作为基准，对比标的相对强弱
- **异动视觉提示**：标的短时间内快速拉升/跳水时，图头出现高亮闪烁提示
- **历史分时回看**：支持选择历史交易日，查看对应日期的板块/个股分时走势
- **量价标注**：可显示日内放量、缩量关键节点
- **所属板块快捷跳转**：个股图表支持一键添加其所属行业、概念板块至对比列表
- **分屏同步缩放**：分屏模式下支持左右两栏时间轴同步缩放

### 模块5：系统设置与配置
- **主题设置**：
  - 支持亮色主题、暗色主题两种风格一键切换，设置自动本地记忆
  - 主题覆盖全部界面元素与图表配色，涨跌色规则在两种主题下保持一致
  - 可选「跟随系统」自动切换亮暗模式
- **行情服务器设置**：支持手动选择通达信服务器节点，或自动优选最快节点
- **刷新间隔设置**：自定义后台自动刷新周期
- **显示设置**：成交量显示开关、昨收线开关、均价线开关、涨停跌停线开关、Y轴模式切换
- **布局设置**：垂直混排 / 左右分屏模式切换
- **配置自动记忆**：所有用户设置（选中标的、布局模式、主题、刷新间隔、显示偏好）自动持久化，下次打开自动恢复
- **自选配置导入导出**：支持自选标的组以配置文件形式备份/恢复
- **单屏数量限制**：可设置最大同时展示标的数量，默认上限15个，避免前端性能过载

---

## 四、数据层性能与风控专项设计
### 1. 连接管理策略
- **单连接复用**：全局维护一条通达信连接，所有请求串行复用，避免频繁建立断开连接
- **节点自动优选**：启动时自动测速多个公开服务器节点，选择延迟最低的节点
- **故障自动转移**：当前节点连接失败或返回异常时，自动切换到备用节点
- **重连退避机制**：连接失败后采用指数退避重试，避免疯狂重连触发风控

### 2. 标的数据适配
- **市场自动识别**：根据标的代码自动判断归属市场（沪市板块/沪市个股/深市个股），自动传入对应Market参数
- **接口统一复用**：分时数据接口复用同一套逻辑，板块与个股仅市场参数差异，底层均使用分时接口
- **批量请求调度**：板块与个股混合请求时统一串行调度，全局共用同一套频率限制规则
- **数据格式归一化**：后端统一返回标准字段结构，前端无需区分标的类型即可渲染
- **板块成分股关系预加载**：启动时一次性加载全量板块-个股对应关系到内存，支撑个股关联板块快速查询

### 3. 缓存体系设计
- **缓存唯一数据源**：前端所有数据均来自内存缓存，后台刷新线程是唯一写入方
- **内存TTL缓存**：按「市场+标的代码」维度缓存实时数据，保证前端请求低延迟
- **本地文件缓存**：当日全量分时数据落地本地文件，服务重启/页面刷新时优先读取，再增量更新
- **缓存失效策略**：交易日切换时自动清空内存与文件缓存，避免跨日数据混淆
- **原子性更新**：单标的数据更新采用原子替换，避免前端读取到半更新数据

### 4. 限流与风控保障
- **全局频率限制**：后端全局控制通达信请求速率，单秒不超过2次请求
- **最低刷新间隔限制**：后台刷新不开放低于15秒的配置，从产品层面规避风险
- **失败熔断机制**：连续多次请求失败时，自动延长刷新间隔，暂停高频请求
- **非交易时段休眠**：周末、节假日、午间休市、收盘后完全停止后台刷新线程，杜绝无效请求

### 5. 数据准确性与异常容错
- **数据完整性校验**：返回数据校验时间点连续性，异常时自动重试
- **异常数据过滤**：对明显异常的价格跳变做过滤处理，避免图表失真
- **单标的降级**：单个标的获取失败时标记异常，保留上一次有效数据，不影响其他标的正常刷新
- **全量失败兜底**：所有节点均连接失败时，暂停刷新并保留最后一份有效数据，前端给出明确提示

---

## 五、前端交互与用户体验设计
### 1. 页面布局
- **左侧面板**（可折叠，固定宽度）：
  - 顶部区域：标的分类Tab切换（板块/个股）、搜索框
  - 中部区域：标的勾选列表、自选标的组管理入口
  - 底部状态区（常驻面板底部）：当前服务器节点、网络状态、最后数据更新时间、已选标的数量
- **顶部工具栏**（横跨主显示区）：
  - 左侧：手动刷新按钮、日期选择器、刷新间隔下拉
  - 中间：布局模式切换（垂直混排 / 左右分屏）
  - 右侧：主题切换按钮（亮/暗/跟随系统）、视图设置
- **主显示区**（自适应宽度）：
  - 垂直混排模式：单列垂直排列分时图表，支持纵向滚动
  - 左右分屏模式：左右两栏分别排列板块、个股分时图表，各自独立滚动

### 2. 核心交互细节
- **悬停联动**：鼠标在任一图表上移动，所有图表同步显示对应时间的十字光标与数据，分屏模式下跨栏联动，左侧列表对应标的同步高亮
- **拖拽排序**：按住图表标题栏可上下拖动调整排列顺序，分屏模式下仅在同栏内拖拽
- **高度调节**：图表底边可拖拽调整单图高度，其他图自动适配
- **快速移除**：每个图表右上角提供关闭按钮，一键从对比列表移除
- **一键全屏**：支持单个标的全屏放大查看
- **快捷添加**：个股图表右键菜单支持快速添加所属行业、概念板块
- **主题无缝切换**：切换亮暗主题时，界面与图表平滑过渡，无闪烁
- **全链路状态反馈**：图表加载中显示骨架屏，单标的加载失败显示灰色占位提示，全量数据异常时左侧状态区高亮告警

### 3. 效率优化
- **键盘快捷键**：支持空格手动刷新、M切换布局模式、T切换主题、上下键调整标的顺序等高频操作
- **图表懒加载**：超出视口的图表暂停渲染，滚动到可视区域再激活
- **增量更新**：刷新时只追加新的分钟数据，不全量重绘
- **DOM复用**：标的数量变化时尽量复用图表实例，减少销毁重建开销
- **时间戳比对**：前端轮询仅比对更新时间戳，无新数据则不发起全量数据拉取

---

## 六、部署与运维方案
### 1. 部署方式
- **主打本地部署**：单Python入口文件启动，浏览器本地访问，无需公网服务器
- **依赖极简**：仅需Python环境 + easy-tdx + FastAPI等少量依赖，支持自动安装
- **配置文件**：所有服务端参数（服务器、端口、刷新阈值）通过本地config.json文件管理

### 2. Windows 静默一键启动脚本（.bat）
仅提供单一静默启动版本，无控制台弹窗，后台运行服务，核心能力如下：
1.  **环境自动检测**：自动校验本地Python环境，缺失时弹出明确提示并终止，避免静默闪退无反馈
2.  **依赖自动安装**：首次运行自动检测依赖包，缺失则后台执行 `pip install` 安装，安装完成后自动启动
3.  **端口自动适配**：默认端口被占用时自动顺延查找可用端口，确保服务正常启动
4.  **编码与异常兼容**：强制设置UTF-8编码，避免中文乱码；启动异常时生成错误日志文件，便于排查
5.  **后台静默运行**：通过 `start /b` + Python无窗口模式启动服务，不显示控制台黑框，进程后台常驻
6.  **自动打开浏览器**：服务启动成功后，自动调用默认浏览器打开应用访问地址
7.  **配套停止脚本**：提供单独的 `停止服务.bat`，一键结束后台运行的Python进程，避免残留进程占用端口

### 3. 运行环境要求
- 系统：Windows 10/11（主打） / macOS / Linux 均可
- Python版本：3.8及以上
- 网络：能正常访问通达信公开服务器即可

### 4. 监控与日志
- 运行日志：自动写入本地log文件，按天切割，记录后台刷新次数、失败次数、节点切换等关键事件
- 异常告警：请求连续失败时在左侧状态区给出高亮提示
- 性能统计：可选展示平均请求耗时、缓存命中率
- 日志导出：左侧状态区支持一键导出运行日志，方便问题排查

---

## 七、开发里程碑与排期
| 阶段 | 周期 | 核心交付物 |
|---|---|---|
| 阶段1：数据验证 | 0.5天 | 打通easy-tdx板块列表、个股搜索、板块/个股分时、板块成分股接口，验证数据准确性与兼容性 |
| 阶段2：后端核心开发 | 1.5天 | 完成标的接口、后台自动刷新线程、缓存体系、交易日历、连接管理、市场适配全部后端逻辑 |
| 阶段3：前端基础版 | 1.5天 | 实现垂直布局、单图渲染、多图联动、基础标的选择、板块+个股混合展示、涨跌幅归一化 |
| 阶段4：功能完善 | 2天 | 左右分屏布局、亮暗双主题、自选标的组、历史分时、设置面板、拖拽排序、快捷添加、配置记忆 |
| 阶段5：打包与交付 | 1天 | 编写静默启动bat脚本、停止脚本、异常容错处理、风控逻辑验证、日志体系、整体体验打磨 |

**合计开发周期约6.5天**，可按优先级分版本迭代。

---

## 八、风险评估与应对方案
| 风险点 | 影响程度 | 应对方案 |
|---|---|---|
| 通达信免费服务器不稳定、偶发断连 | 中 | 多节点备选池 + 自动故障转移 + 异常重试机制 + 本地数据兜底 |
| 请求频率过高触发IP限流 | 中 | 后台单线程串行刷新 + 全局频率控制 + 最低刷新间隔限制 + 非交易时段休眠 |
| 部分小众板块/新股无对应数据 | 低 | 标的列表做可用性标记，支持手动添加代码 + 异常友好提示 |
| 同时展示标的过多导致前端卡顿 | 中 | 图表懒加载 + 轻量化图表库 + 单屏最大数量限制 + 引导合理控制对比数量 |
| 标的数量多导致刷新变慢 | 中 | 串行请求错峰 + 分批刷新 + 缓存统一输出 |
| 静默启动闪退无感知 | 低 | 启动前环境校验 + 异常生成错误日志 + 启动成功自动打开浏览器作为反馈 |
| 通达信协议变更导致接口失效 | 低 | 预留数据源抽象层，可快速替换为pytdx等其他兼容方案 |
| 冷启动加载慢影响体验 | 低 | 首屏优先加载 + 本地文件缓存 + 骨架屏过渡 |

---

## 九、后续可扩展方向
1.  增加K线周期切换（日K、周K、月K）
2.  支持板块资金流向、涨速榜等衍生数据展示
3.  增加自定义价格异动、涨跌幅预警功能
4.  支持多组自选方案快速切换
5.  移动端响应式适配
6.  支持导出分时数据为CSV文件
7.  增加分时叠加对比模式（多标的线叠加在同一图中）
